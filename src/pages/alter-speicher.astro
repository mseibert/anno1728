---
export const prerender = true;
import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import Lightbox from '../components/Lightbox.astro';
import img01 from '../assets/images/speicher/IMG_9763.jpg';
import img02 from '../assets/images/speicher/IMG_9766.jpg';
import img03 from '../assets/images/speicher/IMG_9767.jpg';
import img04 from '../assets/images/speicher/IMG_9769.jpg';
import img05 from '../assets/images/speicher/IMG_9771.jpg';
import img06 from '../assets/images/speicher/IMG_9772.jpg';
import img07 from '../assets/images/speicher/IMG_9773.jpg';
import img08 from '../assets/images/speicher/IMG_9774.jpg';
import img09 from '../assets/images/speicher/IMG_9776.jpg';
import img10 from '../assets/images/speicher/IMG_9779.jpg';
import img11 from '../assets/images/speicher/IMG_9780.jpg';
import img12 from '../assets/images/speicher/IMG_9781.jpg';
import img13 from '../assets/images/speicher/IMG_9794.jpg';

const gallery = [
	{ image: img01, alt: 'Hofansicht mit traditionellen Fachwerkhäusern in Wiesbaden-Auringen' },
	{ image: img02, alt: 'Innenhof mit Außentreppe zum Alten Speicher' },
	{ image: img03, alt: 'Fachwerkhaus mit Metalltreppe und historischer Bruchsteinmauer' },
	{ image: img04, alt: 'Detail der Fachwerkfassade mit roten Holzbalken und weißem Putz' },
	{ image: img05, alt: 'Heller Wohn-Schlafbereich mit Doppelbett und Arbeitsplatz' },
	{ image: img06, alt: 'Modernes Badezimmer mit Waschbecken und großem Spiegel' },
	{ image: img07, alt: 'Badezimmer mit ebenerdiger Dusche und WC' },
	{ image: img08, alt: 'Geräumige Dusche mit Glaswand und Handbrause' },
	{ image: img09, alt: 'Gemütlicher Schlafbereich mit Metallbett und beleuchteter Bruchsteinwand' },
	{ image: img10, alt: 'Rustikale Bruchsteinwand mit LED-Beleuchtung und Holzbalken' },
	{ image: img11, alt: 'Mobile Klimaanlage für angenehme Temperaturen im Sommer' },
	{ image: img12, alt: 'Offener Wohnraum mit Bett, Küchenzeile und antikem Sessel' },
	{ image: img13, alt: 'Straßenansicht der historischen Fachwerkhäuser in Auringen' },
];
const images = gallery.map(g => g.image.src);
---

<Layout title="Alter Speicher - Anno 1728">
	<div class="page-header">
		<h1>Alter Speicher</h1>
		<p class="subtitle">Gemütliche Ferienwohnung mit rustikalem Charme</p>
	</div>

	<div class="slideshow">
		<button class="slide-arrow prev" onclick="changeSlide(-1)">&#10094;</button>
		<div class="slides-container">
			{gallery.map((img, index) => (
				<div class={`slide ${index === 0 ? 'active' : ''}`}>
					<Image
						src={img.image}
						alt={img.alt}
						loading="eager"
						onclick={`openGalleryLightbox(${index})`}
						style="cursor: pointer;"
					/>
				</div>
			))}
		</div>
		<button class="slide-arrow next" onclick="changeSlide(1)">&#10095;</button>
		<div class="slide-counter"><span id="currentSlide">1</span> / {images.length}</div>
		<div class="click-hint">Klicken für Vollbild</div>
	</div>

	<div class="intro-text">
		<p>
			Der Alte Speicher ist unsere charmante kleine Ferienwohnung in Wiesbaden-Auringen.
			Perfekt für Paare oder Alleinreisende, die das besondere Ambiente eines
			historischen Gebäudes genießen möchten. Hier verbinden sich rustikaler Charme
			mit modernem Komfort.
		</p>
	</div>

	<section class="highlights-section">
		<h2>Highlights</h2>
		<div class="highlights-grid">
			<div class="highlight-card">
				<span class="highlight-icon">👥</span>
				<h3>Für 2 Personen</h3>
				<p>Ideal für Paare oder Alleinreisende</p>
			</div>
			<div class="highlight-card">
				<span class="highlight-icon">🏡</span>
				<h3>Rustikales Ambiente</h3>
				<p>Historischer Charme trifft modernen Komfort</p>
			</div>
			<div class="highlight-card">
				<span class="highlight-icon">🌅</span>
				<h3>Ruhige Lage</h3>
				<p>Entspannen Sie in idyllischer Umgebung</p>
			</div>
		</div>
	</section>

	<div class="cta-box">
		<p>Entdecken Sie den Charme des Alten Speichers!</p>
		<div class="cta-buttons">
			<a href="/buchungen/" class="button primary">Jetzt buchen</a>
			<a href="/vorderhaus/" class="button secondary">Zum Vorderhaus</a>
		</div>
	</div>

	<Lightbox />
</Layout>

<style>
	.page-header {
		text-align: center;
		margin-bottom: 2rem;
	}

	.page-header h1 {
		font-size: 2.2rem;
		color: #333;
		margin-bottom: 0.25rem;
	}

	.subtitle {
		color: #666;
		font-size: 1.1rem;
	}

	/* Slideshow */
	.slideshow {
		position: relative;
		border-radius: 12px;
		overflow: hidden;
		margin-bottom: 2rem;
		box-shadow: 0 8px 30px rgba(0,0,0,0.12);
		background: #000;
	}

	.slides-container {
		position: relative;
		width: 100%;
		height: 400px;
	}

	.slide {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		opacity: 0;
		transition: opacity 0.5s ease-in-out;
	}

	.slide.active {
		opacity: 1;
	}

	.slide img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.slide-arrow {
		position: absolute;
		top: 50%;
		transform: translateY(-50%);
		background: rgba(0, 0, 0, 0.5);
		color: #fff;
		border: none;
		padding: 1rem 0.75rem;
		font-size: 1.5rem;
		cursor: pointer;
		z-index: 10;
		transition: background 0.2s;
	}

	.slide-arrow:hover {
		background: rgba(0, 0, 0, 0.8);
	}

	.slide-arrow.prev {
		left: 0;
		border-radius: 0 6px 6px 0;
	}

	.slide-arrow.next {
		right: 0;
		border-radius: 6px 0 0 6px;
	}

	.slide-counter {
		position: absolute;
		bottom: 12px;
		left: 12px;
		background: rgba(0, 0, 0, 0.6);
		color: #fff;
		font-size: 0.75rem;
		padding: 0.25rem 0.6rem;
		border-radius: 4px;
		z-index: 10;
	}

	.click-hint {
		position: absolute;
		bottom: 12px;
		right: 12px;
		background: rgba(0, 0, 0, 0.6);
		color: #fff;
		font-size: 0.75rem;
		padding: 0.25rem 0.6rem;
		border-radius: 4px;
		z-index: 10;
	}

	@media (max-width: 600px) {
		.slides-container {
			height: 250px;
		}

		.slide-arrow {
			padding: 0.75rem 0.5rem;
			font-size: 1.2rem;
		}
	}

	.intro-text {
		background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
		padding: 1.5rem;
		border-radius: 10px;
		border-left: 4px solid #8B2332;
		margin-bottom: 2rem;
	}

	.intro-text p {
		margin: 0;
		color: #444;
		font-size: 1.05rem;
		line-height: 1.7;
	}

	h2 {
		font-size: 1.4rem;
		color: #333;
		margin-bottom: 1.25rem;
		text-align: center;
	}

	.highlights-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
		gap: 1rem;
		margin-bottom: 2rem;
	}

	.highlight-card {
		background: #fff;
		border: 1px solid #e0e0e0;
		border-radius: 10px;
		padding: 1.5rem 1rem;
		text-align: center;
		transition: transform 0.2s, box-shadow 0.2s;
	}

	.highlight-card:hover {
		transform: translateY(-3px);
		box-shadow: 0 6px 20px rgba(0,0,0,0.1);
	}

	.highlight-icon {
		font-size: 2.5rem;
		display: block;
		margin-bottom: 0.75rem;
	}

	.highlight-card h3 {
		font-size: 1rem;
		color: #333;
		margin-bottom: 0.25rem;
	}

	.highlight-card p {
		font-size: 0.85rem;
		color: #666;
		margin: 0;
	}

	.cta-box {
		background: linear-gradient(135deg, #8B2332 0%, #6B1A26 100%);
		color: #fff;
		padding: 2rem;
		border-radius: 12px;
		text-align: center;
	}

	.cta-box p {
		margin-bottom: 1.25rem;
		font-size: 1.1rem;
	}

	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.button {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		border-radius: 6px;
		text-decoration: none;
		font-weight: 500;
		font-size: 0.95rem;
		transition: transform 0.2s;
	}

	.button:hover {
		transform: translateY(-2px);
	}

	.button.primary {
		background: #8B2332;
		color: #fff;
	}

	.button.secondary {
		background: rgba(255,255,255,0.15);
		color: #fff;
		border: 1px solid rgba(255,255,255,0.3);
	}

	.button.secondary:hover {
		background: rgba(255,255,255,0.25);
	}
</style>

<script define:vars={{ images }}>
	let currentIndex = 0;
	const slides = document.querySelectorAll('.slide');
	const totalSlides = slides.length;
	const counter = document.getElementById('currentSlide');

	function showSlide(index) {
		if (index >= totalSlides) index = 0;
		if (index < 0) index = totalSlides - 1;

		slides.forEach(slide => slide.classList.remove('active'));
		slides[index].classList.add('active');
		currentIndex = index;
		if (counter) counter.textContent = currentIndex + 1;
	}

	window.changeSlide = function(direction) {
		showSlide(currentIndex + direction);
	}

	window.openGalleryLightbox = function(index) {
		window.openLightbox(images, index);
	}
</script>
